#include <stdio.h>
#include <stdlib.h>

// struct course
// {
//     int id;
//     int next_id, prior_id;
//     struct course *next;
//     struct course *prior;
//     char name[101];
// };


int main(){
    int num;
    struct course list;
    scanf("%d", &num);
    for(int i = 0; i < num-1; i++){
        list.;
        // if(i == 0){
        //     list.prior = NULL;
        // }
        // else if(i == num-2){
        //     list.next = NULL;
        // }
        // &list = list.next;
    }

    return 0;
}