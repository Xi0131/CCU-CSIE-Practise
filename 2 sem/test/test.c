#include <stdio.h>
#include <string.h>

int main(){
    // char sentence[100];
    // gets(sentence);
    // char tgt[100];
    // gets(tgt);
    // printf("%s\n", sentence);
    // printf("%s", tgt);

    int c;
    c = getchar();
    putchar(c);

    return 0;
}