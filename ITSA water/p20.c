#include <stdio.h>

int main(){

    int n;
    char a[30], b[30], c[30], tmp[30];
    scanf("%d", &n);
    for(int i = 0; i < n; i++){
        scanf("%s", tmp);
        int j = 0;
        while(tmp[j] != '\0'){
            j++;
        }
    }

    return 0;
}