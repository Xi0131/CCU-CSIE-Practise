nt M;
    // printf("What is the size of the matrix: ");
    // scanf("%d", &M);
    // int arr[M][M];
    // printf("Please enter the matrix: ");
    // // for(int *p = &arr[0][0]; p <= &arr[M][M]; p++)
    // // {
    //     // scanf("%d", &*p);
    //     // printf("asd");
    // // }
    // for(int i = 0; i < M; ++i)
    //     for(int j = 0; j < M; ++j)
    //         scanf("%d", &arr[j][i]);
    // for(int i = 0; i < M; ++i)
    //     for(int j = M-1; j >= 0; --j)
    //         